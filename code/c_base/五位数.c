#include <stdio.h>
#include <math.h>
void main()
{
	int a,b,c,count,i;
	count=0;
	scanf("%d",&a);
	c=a/100000;
	if (c!=0)
		printf("error");
      for(i=1;i<=10000;i=i*10)
	  {
		  b=a/i;
		  if(b!=0)
			  count=count+1;}
		  printf("%d",count);
		  for(j=count,k=1;j=1,k=count;j--,k++)
		  {
			  d=(a/pow(10,j))%pow(10,k-1);
			  e=

	  
}